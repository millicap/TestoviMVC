@model IEnumerable<TestoviMVC.Models.MojiTestoviViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Moji testovi</h2>

@if (Model.Count() == 0)
{
   <p>Trenutno nemate polozenih testova</p>
}
else
{

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazivTesta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatumPolaganja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BrojOsvojenihBodova)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NazivTesta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatumPolaganja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BrojOsvojenihBodova)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
    </tr>
}

</table>
}